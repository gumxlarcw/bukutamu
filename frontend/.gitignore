# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env files (per-machine config; keep only the committed template)
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local backup artifacts (CLAUDE.md per-file backup rule — never commit)
*.backup
*.backup2

# Large media (kept on disk for deploy, but too big for git)
public/video/
public/hls/
